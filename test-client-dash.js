import puppeteer from 'puppeteer'; async function test() { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:3000/login', {waitUntil:'networkidle0'}); await page.type('input[type=email]', 'owner@test.com'); await page.type('input[type=password]', 'password123'); await page.click('button[type=submit]'); await page.waitForNavigation({waitUntil:'networkidle0'}); const dashHtml = await page.content(); console.log('Dashboard has DASHBOARD_LOGO:', dashHtml.includes('blue-logo')); await browser.close(); } test();  
